---
// src/core/componentes/Topbar.astro
// 🏁 Barra Superior de Certificación MTC y WhatsApp Oficial (Consume core/idioma/)
import app from '../../app.js';
import esJson from '../idioma/es.json';
import enJson from '../idioma/en.json';

interface Props {
  idioma?: 'es' | 'en';
}

const { idioma = 'es' } = Astro.props;
const t = idioma === 'en' ? enJson.topbar : esJson.topbar;
---

<div class="topbar-container">
  <div class="container-wide topbar-wrapper">
    <div class="topbar-left">
      <i class="fa-solid fa-shield-halved"></i>
      <span id="txt_topbar_cert">{t.certificacion}</span>
    </div>
    <div class="topbar-right topbar-hide-mobile">
      <span><i class="fa-solid fa-location-dot"></i> {t.base}</span>
      <a href={app.whatsappUrl} target="_blank" rel="noopener noreferrer" class="topbar-phone-pill">
        <i class="fa-brands fa-whatsapp"></i> {app.telefono}
      </a>
    </div>
  </div>
</div>
